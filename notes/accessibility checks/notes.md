how to make accessibility chacks part of your e2e tests
@axe-core/playwright(package)-this package abstracts away(hide) all the low level axe calls &&& guarantees that accessibility checks are run in iframes
//which we might need to run if we didnt use npm package and axe repo
then 1.script to include axe 2.axe().run()//

NOW install axe-core from npm 
"""""""""""""""""""""""""""""""""""""""""""""""""""""""""""'
